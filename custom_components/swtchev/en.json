{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Swtch EV Charger",
        "description": "Enter the charger's IP address, API token, and polling settings.",
        "data": {
          "host": "IP address",
          "api_token": "API token (Bearer token)",
          "scan_interval": "Scan interval (seconds)",
          "timeout": "Timeout (seconds)"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the charger",
      "invalid_response": "The charger returned an invalid response",
      "invalid_auth": "The charger rejected the API token",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This charger is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Swtch EV Charger options",
        "description": "Update the API token and polling settings.",
        "data": {
          "api_token": "API token (Bearer token)",
          "scan_interval": "Scan interval (seconds)",
          "timeout": "Timeout (seconds)"
        }
      }
    }
  }
}
